/* ════════════════════════════════════════════════════
   Investment Returns Panel — v2.0
   ThingsBoard v4.3.0 PE | Latest Values
   3 sub-cards: IRR, NPV, ROE
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & TOKENS
   ───────────────────────────── */
.invest-panel {
    /* Accent — cyan default */
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.7);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.22);

    /* Background */
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);

    /* Text hierarchy */
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    /* Semantic */
    --c-good: #66BB6A;
    --c-warning: #FFC107;
    --c-critical: #FF5252;

    /* Surfaces */
    --c-card-bg: rgba(255, 255, 255, 0.04);
    --c-card-border: rgba(255, 255, 255, 0.08);
    --c-card-hover: rgba(255, 255, 255, 0.09);
    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    /* Layout — absolute fill */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.45em 0.6em 0.35em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.invest-panel:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1;
    margin-bottom: 0.2em;
    min-height: 0;
}

.card-title {
    font-size: 0.7em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
}

.header-right {
    display: flex;
    align-items: center;
    gap: 0.25em;
    flex-shrink: 0;
    padding-left: 0.4em;
}

/* ─── Status Dot ─── */
.status-dot {
    width: 0.4em;
    height: 0.4em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    display: inline-block;
    transition: background-color 0.4s ease, box-shadow 0.4s ease;
}

.status-dot.good {
    background-color: var(--c-good);
    box-shadow: 0 0 6px rgba(102, 187, 106, 0.6);
}

.status-dot.warning {
    background-color: var(--c-warning);
    box-shadow: 0 0 6px rgba(255, 193, 7, 0.6);
}

.status-dot.critical {
    background-color: var(--c-critical);
    box-shadow: 0 0 6px rgba(255, 82, 82, 0.6);
    animation: invest-pulse-critical 1.8s infinite;
}

.status-text {
    font-size: 0.45em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
    transition: color 0.3s ease;
}

.status-text.good {
    color: var(--c-good);
}

.status-text.warning {
    color: var(--c-warning);
}

.status-text.critical {
    color: var(--c-critical);
}

/* ─────────────────────────────
   BODY — 3 Sub-Cards
   ───────────────────────────── */
.cards-container {
    flex: 1 1 auto;
    display: flex;
    align-items: stretch;
    gap: 0.4em;
    z-index: 2;
    min-height: 0;
}

.sub-card {
    flex: 1;
    background: var(--c-card-bg);
    border: 1px solid var(--c-card-border);
    border-radius: 6px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 0.4em 0.35em;
    position: relative;
    cursor: pointer;
    transition: background-color 0.25s ease, border-color 0.25s ease;
    overflow: hidden;
    min-width: 0;
}

.sub-card:hover {
    background: var(--c-card-hover);
    border-color: rgba(255, 255, 255, 0.15);
}

/* ─── Sub-Card Typography ─── */
.sub-title {
    font-size: 0.55em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    text-align: center;
    margin-bottom: 0.2em;
    letter-spacing: 0.03em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    width: 100%;
    line-height: 1;
}

.sub-value {
    font-size: 1.6em;
    font-weight: 700;
    color: var(--c-accent);
    line-height: 1;
    margin-bottom: 0.06em;
    white-space: nowrap;
    text-shadow: 0 0 8px var(--c-accent-glow);
    transition: color 0.3s ease, text-shadow 0.3s ease;
}

/* Value sign coloring */
.sub-value.positive {
    color: var(--c-good);
    text-shadow: 0 0 8px rgba(102, 187, 106, 0.15);
}

.sub-value.negative {
    color: var(--c-critical);
    text-shadow: 0 0 8px rgba(255, 82, 82, 0.15);
}

.sub-footer {
    font-size: 0.42em;
    font-weight: 500;
    color: var(--c-text-muted);
    text-align: center;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    width: 100%;
    line-height: 1.2;
}

/* ─── Per-Card Tooltip ─── */
.sub-tooltip {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.3em 0.4em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    border-radius: 0 0 6px 6px;
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 5;
    pointer-events: none;
}

.sub-card:hover .sub-tooltip {
    opacity: 1;
    transform: translateY(0);
}

.sub-tt-label {
    font-size: 0.42em;
    font-weight: 600;
    color: rgba(0, 0, 0, 0.65);
    margin-bottom: 0.1em;
}

.sub-tt-value {
    font-size: 0.55em;
    font-weight: 700;
    color: #1a1a2e;
}

/* ─────────────────────────────
   FOOTER
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    z-index: 2;
    margin-top: 0.15em;
    padding-top: 0.1em;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    min-height: 0;
}

.card-footer {
    font-size: 0.42em;
    font-weight: 500;
    color: var(--c-text-muted);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    opacity: 0.7;
}

/* ─────────────────────────────
   CARD-LEVEL TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.35em 0.5em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.invest-panel:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.45em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   SKELETON & ANIMATIONS
   ───────────────────────────── */
.skeleton {
    opacity: 0.5;
    animation: invest-pulse-load 1.5s infinite;
}

@keyframes invest-pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}

@keyframes invest-pulse-critical {
    0% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(255, 82, 82, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }
}