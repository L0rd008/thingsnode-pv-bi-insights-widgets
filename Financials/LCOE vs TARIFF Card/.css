/* ════════════════════════════════════════════════════
   LCOE vs TARIFF Card
   ThingsBoard v4.3.0 PE | Latest Values
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & COLOR TOKENS
   ───────────────────────────── */
.lcoe-card {
    /* Accent */
    --c-accent: #06F5FF;
    --c-accent-dim: rgba(6, 245, 255, 0.5);
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.8);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.25);

    /* Background */
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);

    /* Text hierarchy */
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    /* Bar colors */
    --c-lcoe: #547aa5;
    --c-lcoe-bright: #6b9fd4;
    --c-tariff: #66BB6A;
    --c-tariff-bright: #81C784;
    --c-tariff-margin: rgba(102, 187, 106, 0.2);
    --c-tariff-margin-border: rgba(102, 187, 106, 0.55);

    /* Status semantics */
    --c-good: #66BB6A;
    --c-warning: #FFC107;
    --c-critical: #FF5252;

    /* Surfaces */
    --c-track: rgba(255, 255, 255, 0.07);
    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    /* ─── Layout: Absolute fill ─── */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.5em 0.7em 0.4em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

/* ─── Hover Glow ─── */
.lcoe-card:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1;
    margin-bottom: 0.2em;
    min-height: 0;
}

.card-title {
    font-size: 0.65em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

/* ─── Status Badge ─── */
.header-status {
    display: flex;
    align-items: center;
    gap: 0.25em;
    flex-shrink: 0;
    padding-left: 0.5em;
}

.status-dot {
    width: 0.4em;
    height: 0.4em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    display: inline-block;
    transition: background-color 0.4s ease, box-shadow 0.4s ease;
}

.status-dot.good {
    background-color: var(--c-good);
    box-shadow: 0 0 6px rgba(102, 187, 106, 0.6);
}

.status-dot.warning {
    background-color: var(--c-warning);
    box-shadow: 0 0 6px rgba(255, 193, 7, 0.6);
}

.status-dot.critical {
    background-color: var(--c-critical);
    box-shadow: 0 0 6px rgba(255, 82, 82, 0.6);
    animation: pulse-critical 1.8s infinite;
}

.status-text {
    font-size: 0.45em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    white-space: nowrap;
}

/* ─────────────────────────────
   BODY — Comparison Bars
   ───────────────────────────── */
.card-body {
    flex: 1 1 auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 0.2em;
    z-index: 2;
    min-height: 0;
}

/* ─── Metric Row ─── */
.metric-row {
    display: flex;
    align-items: center;
    height: 1.5em;
    min-height: 0;
}

/* Labels (Left) */
.metric-label {
    width: 4.5em;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    justify-content: center;
    padding-right: 0.4em;
    text-align: right;
    flex-shrink: 0;
}

.label-main {
    font-size: 0.5em;
    font-weight: 700;
    color: var(--c-text);
    line-height: 1.15;
    white-space: nowrap;
    letter-spacing: 0.03em;
}

.label-sub {
    font-size: 0.35em;
    color: var(--c-text-muted);
    line-height: 1.15;
    white-space: nowrap;
}

/* ─── Bar Visualization ─── */
.metric-bar {
    flex: 1 1 auto;
    height: 100%;
    display: flex;
    align-items: center;
    padding-right: 0.4em;
    min-width: 0;
}

.bar-track {
    flex: 1;
    height: 0.8em;
    background: var(--c-track);
    border-radius: 0.15em;
    display: flex;
    align-items: stretch;
    overflow: hidden;
    position: relative;
}

.bar-fill {
    height: 100%;
    transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    min-width: 0;
}

/* LCOE Bar — Blue gradient */
.bar-lcoe {
    background: linear-gradient(90deg, var(--c-lcoe) 0%, var(--c-lcoe-bright) 100%);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.15);
    border-radius: 0.15em;
    width: 0%;
}

/* Tariff — Cost portion (solid green) */
.bar-cost {
    background: linear-gradient(90deg, var(--c-tariff) 0%, var(--c-tariff-bright) 100%);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.15);
    border-radius: 0.15em 0 0 0.15em;
    width: 0%;
}

/* Tariff — Margin portion (dashed green) */
.bar-profit {
    background: var(--c-tariff-margin);
    border: 1px dashed var(--c-tariff-margin-border);
    border-left: none;
    box-sizing: border-box;
    border-radius: 0 0.15em 0.15em 0;
    width: 0%;
}

/* ─── Metric Value (Right) ─── */
.metric-value {
    min-width: 5em;
    font-size: 0.6em;
    font-weight: 700;
    color: var(--c-text);
    white-space: nowrap;
    text-align: right;
    flex-shrink: 0;
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.08);
}

/* ─────────────────────────────
   FOOTER — Margin Summary
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    margin-top: 0.15em;
    padding-top: 0.15em;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
    min-height: 0;
    gap: 0.3em;
}

/* Delta Section */
.footer-delta {
    display: flex;
    align-items: center;
    gap: 0.15em;
    flex-shrink: 0;
}

.delta-arrow {
    font-size: 0.5em;
    font-weight: 700;
    line-height: 1;
    color: var(--c-text-dim);
    transition: color 0.3s ease;
}

.delta-arrow.positive {
    color: var(--c-good);
}

.delta-arrow.negative {
    color: var(--c-critical);
}

.delta-pct {
    font-size: 0.55em;
    font-weight: 700;
    color: var(--c-text-dim);
    white-space: nowrap;
    transition: color 0.3s ease;
}

.delta-pct.positive {
    color: var(--c-good);
}

.delta-pct.negative {
    color: var(--c-critical);
}

.delta-tag {
    font-size: 0.35em;
    font-weight: 500;
    color: var(--c-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
}

/* Separator */
.footer-sep {
    width: 1px;
    height: 0.7em;
    background: rgba(255, 255, 255, 0.1);
    flex-shrink: 0;
}

/* Margin Absolute Value */
.footer-margin {
    font-size: 0.5em;
    font-weight: 600;
    color: var(--c-text-dim);
    white-space: nowrap;
    text-align: center;
    flex: 0 1 auto;
    overflow: hidden;
    text-overflow: ellipsis;
}

/* Ratio Section */
.footer-ratio {
    display: flex;
    align-items: center;
    gap: 0.2em;
    flex-shrink: 0;
}

.ratio-track {
    width: 2.5em;
    height: 0.35em;
    background: var(--c-track);
    border-radius: 0.1em;
    overflow: hidden;
    display: flex;
}

.ratio-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--c-lcoe) 0%, var(--c-accent-dim) 100%);
    border-radius: 0.1em;
    transition: width 0.5s ease;
    width: 0%;
}

.ratio-text {
    font-size: 0.38em;
    color: var(--c-text-muted);
    white-space: nowrap;
    font-weight: 600;
}

/* ─────────────────────────────
   TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4em 0.6em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.lcoe-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.5em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   LOADING SKELETON
   ───────────────────────────── */
.skeleton {
    opacity: 0.5;
    animation: pulse-load 1.5s infinite;
}

@keyframes pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}

/* ─────────────────────────────
   CRITICAL STATUS PULSE
   ───────────────────────────── */
@keyframes pulse-critical {
    0% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(255, 82, 82, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }
}
